# Python Project Environment Variables
PYTHON_VERSION=3.9
VENV_PATH=${DEVPROJECTS_ROOT}/active/${PROJECT_NAME}/.venv
PYTHONPATH=${DEVPROJECTS_ROOT}/active/${PROJECT_NAME}/src
PIP_CACHE_DIR=${DEVPROJECTS_ROOT}/.pip-cache

# Testing Configuration
PYTEST_ADDOPTS=--color=yes --capture=no
COVERAGE_FILE=${DEVPROJECTS_ROOT}/active/${PROJECT_NAME}/.coverage

# Documentation
# PYTHON_VERSION: Python version for the project
# VENV_PATH: Virtual environment location
# PYTHONPATH: Python module search path
# PIP_CACHE_DIR: pip package cache location
# PYTEST_ADDOPTS: pytest configuration
# COVERAGE_FILE: Coverage report location
